{"path":"fixtures/synthetic-worker.ts","content":"// Synthetic TypeScript declarations for embedding-outline tests.\nexport interface WorkItem {\n  id: string;\n  run(): Promise<void>;\n}\n\nexport type WorkResult = {\n  ok: boolean;\n};\n\nexport {\n  type ExternalShape,\n  externalHelper,\n} from './external.js';\n\nexport function buildSyntheticWorker(label: string): SyntheticWorker {\n  const x = 1;\n  const arrowTask = (value: number) => value + x;\n  arrowTask(x);\n  return SyntheticWorker.create(label);\n}\n\nexport class SyntheticWorker {\n  private state = 'idle';\n\n  constructor(readonly label: string) {}\n\n  async execute(input: string): Promise<WorkResult> {\n    this.state = input;\n    return { ok: true };\n  }\n\n  static create(label: string): SyntheticWorker {\n    return new SyntheticWorker(label);\n  }\n\n  private reset(): void {\n    this.state = 'idle';\n  }\n\n  transform<TValue>(value: TValue): TValue {\n    return value;\n  }\n\n  async processExtremelyDetailedRequest<TValue>(firstArgument: TValue, secondArgument: { requestIdentifier: string; retryCount: number; traceLabel: string }, thirdArgument: string, fourthArgument: boolean): Promise<TValue> {\n    return firstArgument;\n  }\n\n  operation01(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation02(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation03(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation04(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation05(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation06(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation07(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation08(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation09(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation10(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation11(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation12(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation13(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation14(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation15(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation16(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation17(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation18(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation19(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation20(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation21(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation22(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation23(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation24(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation25(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation26(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation27(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n\n  operation28(input: string, context: { requestId: string; attempt: number }): string {\n    return `${context.requestId}:${context.attempt}:${input}`;\n  }\n}","expected":"fixtures/synthetic-worker.ts\nSynthetic TypeScript declarations for embedding-outline tests. | export interface WorkItem {\nexport interface WorkItem {\nrun(): Promise<void>;\nexport type WorkResult = {\nexport function buildSyntheticWorker(label: string): SyntheticWorker {\nconst arrowTask = (value: number) => value + x;\nexport class SyntheticWorker {\nconstructor(readonly label: string) {}\nasync execute(input: string): Promise<WorkResult> {\nstatic create(label: string): SyntheticWorker {\nprivate reset(): void {\ntransform<TValue>(value: TValue): TValue {\nasync processExtremelyDetailedRequest<TValue>(firstArgument: TValue, secondArgument: { requestIdentifier: string; retryCount: number; traceLabel: string }, thir\noperation01(input: string, context: { requestId: string; attempt: number }): string {\noperation02(input: string, context: { requestId: string; attempt: number }): string {\noperation03(input: string, context: { requestId: string; attempt: number }): string {\noperation04(input: string, context: { requestId: string; attempt: number }): string {\noperation05(input: string, context: { requestId: string; attempt: number }): string {\noperation06(input: string, context: { requestId: string; attempt: number }): string {\noperation07(input: string, context: { requestId: string; attempt: number }): string {\noperation08(input: string, context: { requestId: string; attempt: number }): string {\noperation09(input: string, context: { requestId: string; attempt: number }): string {\noperation10(input: string, context: { requestId: string; attempt: number }): string {\noperation11(input: string, context: { requestI\n// Synthetic TypeScript declarations for embedding-outline tests.\nexport interface WorkItem {\n  id: string;\n  run(): Promise<void>;\n}\n\nexport type WorkResult = {\n  ok: boolean;\n};\n\nexport {\n  type ExternalShape,\n  externalHelper,\n} from './external.js';\n\nexport function buildSyntheticWorker(label: string): SyntheticWorker {\n  const x = 1;\n  const arrowTask = (value: number) => value + x;\n  arrowTask(x);\n  return SyntheticWorker.create(label);\n}\n\nexport class SyntheticWorker {\n  private state = '"}
