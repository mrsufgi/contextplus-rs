{"path":"fixtures/synthetic-worker.test.ts","content":"// Synthetic TypeScript test declarations for embedding-outline tests.\nimport { beforeEach, describe, expect, it } from 'test-kit';\n\ndescribe('synthetic worker', () => {\n  beforeEach(() => {\n    resetSyntheticState();\n  });\n\n  it('runs a task', async () => {\n    registerHook(createHook());\n    const result = await runSyntheticTask();\n    expect(result).toBe(true);\n  });\n\n  it('supports another case', () => {\n    verifySyntheticResult();\n  });\n});\n","expected":"fixtures/synthetic-worker.test.ts\nSynthetic TypeScript test declarations for embedding-outline tests. | describe('synthetic worker', () => {\nbeforeEach(() => {\nit('runs a task', async () => {\nit('supports another case', () => {\n// Synthetic TypeScript test declarations for embedding-outline tests.\nimport { beforeEach, describe, expect, it } from 'test-kit';\n\ndescribe('synthetic worker', () => {\n  beforeEach(() => {\n    resetSyntheticState();\n  });\n\n  it('runs a task', async () => {\n    registerHook(createHook());\n    const result = await runSyntheticTask();\n    expect(result).toBe(true);\n  });\n\n  it('supports another case', () => {\n    verifySyntheticResult();\n  });\n});\n"}
